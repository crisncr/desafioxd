<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Iniciar Sesión</title>
</head>
<body>
  <h1>Iniciar Sesión</h1>

  <!-- Mostrar error si existe -->
  <% if (errorMessage) { %>
    <div style="color: red; background-color: #f8d7da; padding: 10px; border-radius: 5px;">
      <strong>Error:</strong> <%= errorMessage %>
    </div>
  <% } %>

  <form action="/users/login" method="POST">
    <div>
      <label for="email">Correo Electrónico:</label>
      <input type="email" id="email" name="email" required>
    </div>
    <div>
      <label for="password">Contraseña:</label>
      <input type="password" id="password" name="password" required>
    </div>
    <button type="submit">Iniciar sesión</button>
  </form>

  <p><a href="/users/request-password-reset">Olvidaste tu contraseña?</a></p>
  <p>¿No tienes cuenta? <a href="/register">Registrar cuenta</a></p> <!-- Enlace para registrar -->
</body>
</html>
